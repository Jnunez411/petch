spring:
  application:
    name: petch-api
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/petchdb?user=postgres&password=Admit#5001
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# JWT Configuration
jwt:
  # IMPORTANT: JWT_SECRET must be set in .env file
  # Generate a secure random key with: openssl rand -base64 64
  secret: r/ryb7TMriZJ+FmCTKrewbc7eHEWMua3OyI5v93VsPAU8j4gtFEGiTJs/PZX10WT
  expiration: 86400000 # 24 hours in milliseconds (1 day)

# CORS Configuration
cors:
  # Comma-separated list of allowed origins
  # Update this to match your frontend URL(s)
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# Upload Configuration
app:
  upload:
    dir: uploads/images
